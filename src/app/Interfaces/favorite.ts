export interface Favorite {
    id: number;
    eventid: number;
    username: string;    
    favorite: boolean;
}

export interface SubmitFavorite {
    id: number;
    eventid: number;
    username: string;    
    favorite: boolean;
}